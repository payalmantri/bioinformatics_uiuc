name: payalmantri/bioinformaticsuiuc
version: 0.0.0
dbscheme: sample/db-python/semmlecode.python.dbscheme
dependencies:
  # This uses the latest version of the codeql/python-all library.
  # You may want to change to a more precise semver string.
  codeql/python-all: "*"